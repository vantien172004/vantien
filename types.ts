export interface Category {
  id: string;
  title: string;
  imageUrl: string;
  backgroundUrl: string;
}

export interface Product {
  id: string;
  title: string;
  price: string;
  imageUrl: string;
}
